use crate::action_result_enum::ActionResult;
use ragit_types::chunk::rendered_chunk::RenderedChunk;

impl ActionResult {
    pub fn render_read_file_short(_rendered: &RenderedChunk) -> String {
        panic!("FIX ME LATER: Fix the bootstrap first and this code later.");
    }
}