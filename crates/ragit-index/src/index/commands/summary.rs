use crate::prelude::*;

pub async fn summary_command(
    _args: Vec<String>,
    _pre_args: ParsedArgs,
) -> Result<(), ApiError> {
    println!("summary_command is not yet implemented");
    Ok(())
}