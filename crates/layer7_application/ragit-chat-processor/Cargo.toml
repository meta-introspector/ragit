[package]
name = "ragit-chat-processor"
version = "0.1.0"
edition = "2021"

[dependencies]
markdown = "1.0.0-alpha.0"
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4.0", features = ["derive"], optional = true }

[features]
cli = ["clap"]