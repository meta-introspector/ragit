use crate::index_struct::Index;
use ragit_types::ApiError;

impl Index {
    pub fn remove_all_meta(&mut self) -> Result<(), ApiError> {
        eprintln!("Placeholder for remove_all_meta");
        Ok(())
    }
}