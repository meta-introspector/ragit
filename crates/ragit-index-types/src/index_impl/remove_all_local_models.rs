use crate::index_struct::Index;
use ragit_error::ApiError;

impl Index {
    pub fn remove_all_local_models(&mut self) -> Result<(), ApiError> {
        eprintln!("Placeholder for remove_all_local_models");
        Ok(())
    }
}