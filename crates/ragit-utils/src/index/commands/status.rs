use crate::prelude::*;

pub async fn status_command(args: Vec<String>, pre_args: crate::cli_types::ParsedArgs) -> Result<(), Error> {
    println!("status_command is not yet implemented");
    Ok(())
}
